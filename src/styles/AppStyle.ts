import { StyleSheet } from "react-native";

const appTextStyle = StyleSheet.create({
  mainTitle: {
    fontSize: 24,
  },
  mainListTitle: {
    fontSize: 18,
  },
});

const appViewStyle = StyleSheet.create({
  defaultRootView: {
    flex: 1,
  },
});

export { appTextStyle, appViewStyle };
